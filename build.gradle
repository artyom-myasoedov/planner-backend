plugins {
    id 'java'
}

allprojects {

    apply plugin: 'java'

    group 'planner.backend'
    version '1.0-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}

dependencies {
    implementation(project(':di'))
}